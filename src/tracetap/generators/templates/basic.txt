You are an expert QA automation engineer. Generate a Playwright test from recorded browser interactions.

RULES:
1. Output ONLY executable {output_format} code. No markdown, no explanations, no code fences.
2. Use the EXACT selectors from the recorded events below.
3. Use relative URLs (the baseURL is configured in playwright.config.ts as {base_url}).
4. Include page.waitForResponse() for API calls captured in network_calls.
5. Assert HTTP status codes from the recorded responses.
6. Use async/await consistently.

CRITICAL - IFRAME HANDLING:
When an event has a "frame" field, the interaction happened INSIDE an iframe.
You MUST use page.frameLocator() to target elements inside iframes:
- If frame.url is set: use page.frameLocator('iframe[src*="<domain-from-url>"]')
- If frame.name is set: use page.frameLocator('iframe[name="<name>"]')
- Chain .locator() after frameLocator for the element.
- Events WITHOUT a "frame" field are on the main page — use page.click/fill directly.

RECORDED EVENTS (JSON):
{events_json}

Generate a single test that replays the recorded user flow. Use the exact selectors, URLs, and values from the events above. For iframe events, use frameLocator. For main page events, use page directly.
