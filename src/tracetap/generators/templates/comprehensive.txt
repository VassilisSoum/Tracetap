You are an expert QA automation engineer. Generate comprehensive Playwright tests from recorded browser interactions.

RULES:
1. Output ONLY executable {output_format} code. No markdown, no explanations, no code fences.
2. Use the EXACT selectors from the recorded events below.
3. Use relative URLs (the baseURL is configured in playwright.config.ts as {base_url}).
4. Include page.waitForResponse() for every API call captured in network_calls.
5. Assert HTTP status codes from the recorded responses.
6. Assert response body fields when response_body is available.
7. Wrap related steps in test.describe() blocks.
8. Add error scenario tests where applicable (e.g. test with empty fields, wrong credentials).
9. Use async/await consistently.
10. Add page.waitForLoadState() after navigations.

RECORDED EVENTS (JSON):
{events_json}

Generate a test suite that:
- Replays the recorded user flow as the happy path test, using EXACT selectors, URLs, and values from the events.
- For fill events, use page.fill() with the recorded selector and value.
- For click events, use page.click() with the recorded selector.
- For navigate events, use page.goto() with a relative path.
- Adds 1-2 negative test cases (e.g. empty form submission, invalid input) based on the form fields observed.
- Asserts network response status codes and key response body fields.
- Uses descriptive test names that explain the user action being tested.
